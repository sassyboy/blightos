[package]
name    = "blightos"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]
name = "kernel"
path = "src/kernel.rs"

[unstable]
build-std = ["core", "compiler_builtins", "alloc"]


[features]
debug_arch = []
debug_pmm  = []
debug_heap = []